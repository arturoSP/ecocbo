% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{run_pilot_rarefaction}
\alias{run_pilot_rarefaction}
\title{Run empirical rarefaction over a pilot-data sampling grid}
\usage{
run_pilot_rarefaction(
  comm_pilot,
  factEnvP,
  pilot_effort_grid,
  n_iter = 999,
  permutations = 999,
  seed = 123,
  method,
  transformation,
  dummy,
  model,
  parallel = FALSE,
  workers = max(1, parallelly::availableCores() - 1),
  progress = TRUE
)
}
\arguments{
\item{comm_pilot}{A data frame, matrix, or object coercible to a data frame
containing the community data. Rows must correspond to the rows in
\code{factEnvP}.}

\item{factEnvP}{A data frame containing the standardized nested design
factors. It must include columns named \code{sector} and \code{site}.}

\item{pilot_effort_grid}{A data frame defining the sampling-effort
combinations to evaluate. It must contain at least the columns \code{effort_id},
\code{m}, \code{n}, \code{total_sites}, \code{total_subsamples}, and \code{prop_full_pilot}.}

\item{n_iter}{Integer. Number of independent subsampling iterations to run for
each row of \code{pilot_effort_grid}.}

\item{permutations}{Integer. Number of permutations used by the internal
PERMANOVA routine for each subsampled dataset.}

\item{seed}{Integer. Random seed used for reproducible sequential execution
and for parallel random-number generation through \code{furrr}.}

\item{method}{Character string specifying the dissimilarity method used by the
internal PERMANOVA workflow. Typically \code{"bray"}.}

\item{transformation}{Character string specifying the transformation applied
to the community data before dissimilarity calculation. Supported values
depend on the internal PERMANOVA helper and typically include \code{"none"},
\code{"square root"}, \code{"fourth root"}, \code{"Log (X+1)"}, and \code{"P/A"}.}

\item{dummy}{Logical. If \code{TRUE}, a dummy variable is added before calculating
dissimilarities.}

\item{model}{Character string specifying the PERMANOVA model. This workflow is
intended for \code{"nested.symmetric"}.}

\item{parallel}{Logical. If \code{TRUE}, the rarefaction jobs are evaluated in
parallel using \code{furrr} and \code{future}. If \code{FALSE}, jobs are evaluated
sequentially.}

\item{workers}{Integer. Number of parallel workers to use when
\code{parallel = TRUE}.}

\item{progress}{Logical. If \code{TRUE}, a text progress bar is displayed through
\code{progressr}.}
}
\value{
A data frame with one row per subsampling iteration and effort combination.
The output includes the sampling-effort metadata from \code{pilot_effort_grid},
the iteration number, the number of sampled rows, the number of sectors, the
number of nested sites, pseudo-F statistics and p-values for the fixed sector
effect and the nested site-within-sector effect, and an \code{error} column
containing diagnostic messages when an iteration fails.
}
\description{
Internal computational engine used by \code{\link[=empirical_power]{empirical_power()}} to estimate
empirical detection probability from pilot data. The function takes a
predefined grid of nested sampling efforts and repeatedly subsamples the
pilot data at each effort combination. For each subsample, it applies the
internal PERMANOVA workflow and stores pseudo-F statistics and p-values for
the fixed factor and the nested factor.
}
\details{
The function assumes that \code{factEnvP} has already been standardized to contain
at least two columns named \code{sector} and \code{site}, where \code{sector} represents the
fixed factor of interest and \code{site} represents the sampling unit nested within
sector. The function preserves all sector levels and varies the number of
sites per sector (\code{m}) and the number of subsamples per site (\code{n}) according
to \code{pilot_effort_grid}.

For each row in \code{pilot_effort_grid}, the function creates \code{n_iter}
independent subsampling jobs. Each job is passed to
\code{run_one_pilot_iteration()}, which performs the nested subsampling and calls
the PERMANOVA wrapper. The output from all jobs is row-bound into a single
results data frame.

If \code{parallel = TRUE}, jobs are evaluated using \code{furrr::future_map_dfr()} with
a \code{future::multisession} backend. If \code{parallel = FALSE}, jobs are evaluated
sequentially using \code{purrr::map_dfr()}. Progress reporting is handled through
the \code{progressr} package.
}
\keyword{internal}
